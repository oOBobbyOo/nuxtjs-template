export enum RouteEnum {
  ROOT_PATH = '/',
  LOGIN_PATH = '/login',
  HOME_PATH = '/dashboard',
}

export enum LoginModuleEnum {
  PWD_LOGIN = 'pwd-login',
  CODE_LOGIN = 'code-login',
  REGISTER = 'register',
  RESET_PWD = 'reset-pwd',
}
